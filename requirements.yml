---
# requirements.yml — External Ansible collections
# Note: Docker role is now self-built under ansible/roles/docker/

collections:
  - name: ansible.posix
    version: ">=2.1.0"
  - name: community.docker
    version: ">=5.0.0"
  - name: community.general
    version: ">=12.4.0"
