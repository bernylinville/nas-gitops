---
# hosts.yml — NAS GitOps production inventory
all:
  hosts:
    nas:
      ansible_host: nas
      ansible_user: kchou
      ansible_become: true
      ansible_python_interpreter: /usr/bin/python3
