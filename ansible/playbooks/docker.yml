---
# docker.yml — Install and configure Docker
# Usage: ansible-playbook -i inventory/prod ansible/playbooks/docker.yml
- name: Install Docker
  hosts: nas
  become: true
  gather_facts: true

  vars:
    docker_users:
      - "{{ nas_ssh_allow_users }}"

  roles:
    - role: docker
