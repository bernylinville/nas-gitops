---
# baseline/tasks/packages.yml — Install baseline packages
- name: "Packages | Update apt cache"
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: "Packages | Install baseline packages"
  ansible.builtin.apt:
    name: "{{ baseline_packages }}"
    state: present
