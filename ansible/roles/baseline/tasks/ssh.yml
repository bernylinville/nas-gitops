---
# baseline/tasks/ssh.yml — SSH hardening
- name: "ssh | Deploy sshd_config"
  ansible.builtin.template:
    src: sshd_config.j2
    dest: /etc/ssh/sshd_config
    mode: "0600"
    owner: root
    group: root
    backup: true
    validate: "sshd -t -f %s"
  notify: Restart sshd
