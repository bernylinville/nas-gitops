---
# monitoring/tasks/main.yml — Monitoring setup
- name: "Monitoring | Include SMART monitoring tasks"
  ansible.builtin.include_tasks:
    file: "{{ role_path }}/tasks/smart.yml"

- name: "Monitoring | Include mdadm monitoring tasks"
  ansible.builtin.include_tasks:
    file: "{{ role_path }}/tasks/mdadm.yml"
